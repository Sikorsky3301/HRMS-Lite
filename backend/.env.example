# Database Configuration
# For local development (SQLite), leave DATABASE_URL empty or unset
# For online database (PostgreSQL), set DATABASE_URL to your PostgreSQL connection string
# Example: postgresql://user:password@host:port/database
# Or: postgres://user:password@host:port/database
DATABASE_URL=

# Optional: Set to 'false' to disable SSL for PostgreSQL (not recommended for production)
# DATABASE_SSL=true

# Server Configuration
PORT=5000
NODE_ENV=development
